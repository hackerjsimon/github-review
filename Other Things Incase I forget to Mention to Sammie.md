## CANNOT USE SCREENSHOTS atleast from what i read, so i'll remeber to let samantha know she can use output logs
in terminal run:
```
python main.py > output_log.txt
```
Or if we can get there we can use, essentially saving the output logs and displaying them rather than manually copying everytime.

```
$ python main.py
[Actual output file directory here]
```

# the basics, VSCODE seems to have priority so we will use that

git init
git add [file_name.extension]
git commit -m " checkpoint and a message in quotes... oh hey im the message hello!" 

git remote add origin [url]
git push -u origin main  [--Pushes to main-- -u links local to remote github -- origin is just a name for local -- main is the remote online repository]
